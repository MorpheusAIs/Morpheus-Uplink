# UPLINK SecretVM secrets - paste as Encrypted Secrets (or fill the form
# SecretVM auto-builds from the compose substitutions).
#
# Replace every YOUR_* / PASTE_* value. Generate the seed with:
#   openssl rand -hex 32
#
# Required (5):
WALLET_PRIVATE_KEY=0xYOUR_PRIVATE_KEY
ETH_NODE_ADDRESS=https://base-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
COOKIE_CONTENT=admin:YOUR_STRONG_PASSWORD
ADMIN_PASSWORD=YOUR_GUI_PASSWORD
API_KEY_SEED=PASTE_openssl_rand_hex_32_HERE

# Optional (form may show these; safe to leave at defaults):
# Public URL + /node so the router's swagger links point at the passthrough.
WEB_PUBLIC_URL=https://your-vm-name.vm.scrtlabs.com/node
# Session length in seconds (default 600). Stake scales with duration.
#SESSION_DURATION_SECONDS=600
